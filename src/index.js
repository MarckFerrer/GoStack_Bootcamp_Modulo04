// Import React to the project
import React from 'react';
// Import the react-dom to manipule the DOM on the HTML files
import { render } from 'react-dom';

import App from './App';

render(<App />, document.getElementById('app'));
